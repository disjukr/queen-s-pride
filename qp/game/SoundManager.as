package qp.game {    import flash.media.Sound;	import flash.media.SoundChannel;    public class SoundManager {        private static var current: SoundChannel;        private static var map: Object = {};        public static function register(id: String, sound: Class): void {            SoundManager.map[id] = sound;        }        public static function event(id: String): SoundChannel {            if (SoundManager.map[id] != null)                return (new SoundManager.map[id]).play();			return null;        }        public static function play(id: String, repeat: Boolean = true): void {            SoundManager.stop();            if (SoundManager.map[id] != null)                current = (new SoundManager.map[id]).play(0, repeat ? 100 : 0);        }        public static function stop(): void {            if (current != null)                current.stop();            current = null;        }    }}